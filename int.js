   // Solicitando ao usuário um número e converta-o para número usando `parseInt()`. Em seguida, exibindo o número convertido no console.

let numero=Number(prompt('Digite um numero real: '));
let numeroInt=parseInt(numero);

 console.log(numeroInt);